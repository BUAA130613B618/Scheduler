#include<stdio.h>
#include<stdlib.h>

#define FIFO "MYFIFO"

int main()
{
	FILE *fp;
	
	if(argc != 2){
		printf("Usage: client [String]\n");
		exit(1);
	}
	if((fp = fopen(FIFO, "w")) == NULL){
		perror("open");
		exit(1);
	}
	
	fputs(argv[1], fp);
	fclose(fp);
	return 0;
}
